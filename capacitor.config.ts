import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cornerstoneathletics.cuconnect',
  appName: 'CU Connect',
  webDir: 'dist'
};

export default config;
